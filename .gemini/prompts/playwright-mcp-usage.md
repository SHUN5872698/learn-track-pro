## 1. ログイン

```markdown
Playwright MCPを使ってLearnTrack Proにログインしてください。

## 手順

1. **ログインページにアクセス**
   - URL: http://localhost:8000/login
   - ページが完全に読み込まれるまで待機してください

2. **browser_fill_formツールを使用してログイン**
   - 以下の認証情報を一度に入力：
     * メールアドレス: test01@example.com
     * パスワード: password
   - browser_fill_formツールで両方のフィールドを同時に設定してください

3. **ログインボタンをクリック**
   - ボタンのテキスト: "ログイン"
   - クリック後、ダッシュボードへのリダイレクトを待機

4. **ログイン成功の確認**
   - URL が http://localhost:8000/dashboard に変わることを確認
   - ページタイトルが "LearnTrack Pro" であることを確認
   - "学習ダッシュボード" の見出しが表示されていることを確認

5. **ブラウザを開いたまま維持**
   - ログイン状態を保持するため、ブラウザを閉じないでください

## 注意事項
- browser_typeではなく、browser_fill_formを使用してください（入力の安定性のため）
- 各ステップ後は必ずページの状態を確認してください
```

---

## 2. スクリーンショット撮影

```markdown
## メモ表示レイアウト問題箇所のスクリーンショット撮影

現在のブラウザでログイン状態を維持したまま、以下の手順でスクリーンショットを撮影してください。

---

## 📱 撮影設定

### デバイスサイズ一覧

以下の各サイズで、全ページのスクリーンショットを撮影します：

1. **デスクトップ**: 1920x1080
   - ファイル名プレフィックス: `desktop-`
   
2. **モバイル**: 375x667
   - ファイル名プレフィックス: `mobile-`

### 撮影の流れ

1. デスクトップサイズ（1920x1080）に設定
2. 全3ページを撮影
3. モバイルサイズ（375x667）に設定
4. 全3ページを撮影

合計6枚のスクリーンショットが生成されます。

---

## 🖥️ Phase 1: デスクトップサイズでの撮影

### ステップ0: ウィンドウサイズ設定

**最初に一度だけ実行**:
- `browser_resize`ツールを使用
- 幅: 1920ピクセル
- 高さ: 1080ピクセル

### 撮影対象ページと手順

**重要**: 各ページで必ず以下の順序で実行してください：
1. ページに移動
2. 5秒間待機（SPAのレンダリング完了を待つ）
3. 対象要素が表示されていることを確認
4. スクリーンショット撮影

---

#### 1. 全体レポート画面

**手順**:
1. ページに移動: http://localhost:8000/reports
2. 5秒間待機してください
3. 「学習記録一覧」または「記録を編集」ボタンが表示されていることを`browser_snapshot`で確認
4. 確認できたら、フルページスクリーンショットを撮影
   - ファイル名: **desktop-reports-memo-display.png**
   - 保存先: resources/js/.playwright-mcp/

---

#### 2. 個別レポート画面

**手順**:
1. ページに移動: http://localhost:8000/learning/1000/section/87
   - **注意**: 学習コンテンツID=1000を使用してください
2. 5秒間待機してください
3. 「学習記録一覧」または「日別学習時間」グラフが表示されていることを`browser_snapshot`で確認
4. 確認できたら、フルページスクリーンショットを撮影
   - ファイル名: **desktop-study-progress-memo-display.png**
   - 保存先: resources/js/.playwright-mcp/

---

#### 3. セクション別記録画面

**手順**:
1. ページに移動: http://localhost:8000/learning/1000/section/87
   - **注意**: セクションID=86は学習記録が複数件存在するため、メモ表示の問題を確認するのに最適です
2. 5秒間待機してください
3. 「学習記録一覧」または「このセクションに記録を追加」ボタンが表示されていることを`browser_snapshot`で確認
4. 確認できたら、フルページスクリーンショットを撮影
   - ファイル名: **desktop-section-records-memo-display.png**
   - 保存先: resources/js/.playwright-mcp/

---

## 📱 Phase 2: モバイルサイズでの撮影

### ステップ0: ウィンドウサイズ変更

**デスクトップ撮影完了後に実行**:
- `browser_resize`ツールを使用
- 幅: 375ピクセル
- 高さ: 667ピクセル

### 撮影対象ページと手順

Phase 1と同じ3ページを、同じ手順で撮影します。
**ファイル名のみが異なります**：

#### 1. 全体レポート画面
- ファイル名: **mobile-reports-memo-display.png**

#### 2. 個別レポート画面
- ファイル名: **mobile-study-progress-memo-display.png**

#### 3. セクション別記録画面
- ファイル名: **mobile-section-records-memo-display.png**

---

## ⚙️ 重要な注意事項

- **必ず5秒間待機してください**（Vue.jsのレンダリングに時間がかかるため）
- スクリーンショット前に`browser_snapshot`でコンテンツの存在を確認してください
- もし白い画面が表示された場合は、さらに3秒待機してから再度スクリーンショットを撮影してください
- **ウィンドウサイズの変更は各Phaseの最初に1回だけ**実行してください

---

## ✅ 完了確認

### 生成されるファイル一覧

**デスクトップ（3枚）**:
- desktop-reports-memo-display.png
- desktop-study-progress-memo-display.png
- desktop-section-records-memo-display.png

**モバイル（3枚）**:
- mobile-reports-memo-display.png
- mobile-study-progress-memo-display.png
- mobile-section-records-memo-display.png

**合計**: 6枚のスクリーンショット

撮影完了後、ファイル名とパスを報告してください。
```

**撮影サイズにカスタマイズ**

```markdown
## 撮影設定（カスタマイズ可能）

### サイズ定義

以下のサイズで撮影します。必要に応じて変更してください：

| デバイス | 幅 | 高さ | プレフィックス |
|---------|-----|------|--------------|
| デスクトップ | 1920px | 1080px | `desktop-` |
| タブレット | 768px | 1024px | `tablet-` |
| モバイル | 375px | 667px | `mobile-` |

### 今回の撮影対象

- [x] デスクトップ（1920x1080）
- [ ] タブレット（768x1024）
- [x] モバイル（375x667）

チェックが入っているサイズのみ撮影してください。
```

---

## 3. 修正サンプル

学習記録一覧のメモ表示レイアウト修正

```markdown
添付画像: desktop-section-records-memo-display.png

この画像を見て、学習記録カードのメモ表示の問題を分析し、修正してください。
### スクリーンショット
- `.playwright-mcp/desktop-section-records-memo-display.png`

### ファイル
`resources/js/views/learning/SectionStudyRecords.vue`

---

## 修正後の確認

修正完了後、以下を厳密に実行してください：

1. ブラウザサイズ: 1920x1080
2. ページ移動: http://localhost:8000/learning/1000/section/87**
3. 5秒待機
4. スクリーンショット撮影: `section-records-AFTER.png`
5. **撮影した画像を見て以下を確認**:
   - メモが複数行で表示されているか
   - ボタンが右側に固定されているか
   - メモに背景色があるか
   - 問題が改善されたか

確認結果を報告してください。
```

---

## 4. レイアウト問題分析プロンプト

レイアウト問題分析

```markdown
添付画像を見て、レイアウトの問題を分析してください。

### スクリーンショット
- `.playwright-mcp/<file_name>`

## チェック項目

### 1. テキスト表示
- [ ] 長文が適切に折り返されているか
- [ ] テキストが要素からはみ出していないか
- [ ] 改行が正しく表示されているか
- [ ] フォントサイズは適切か

### 2. 要素の配置
- [ ] ボタンやアイコンが見える位置にあるか
- [ ] 要素同士が重なっていないか
- [ ] 要素が意図しない位置に落ちていないか
- [ ] 余白は適切か

### 3. レスポンシブ対応
- [ ] カード内の要素が適切に収まっているか
- [ ] 横幅が制限されすぎていないか
- [ ] 縦方向のスペースは適切か

### 4. 視覚的な区別
- [ ] 異なる情報が明確に区別できるか
- [ ] 背景色やボーダーは適切か
- [ ] 重要な情報が目立っているか

### 5. ユーザビリティ
- [ ] クリック可能な要素が明確か
- [ ] 情報の優先順位が視覚的に表現されているか
- [ ] スクロールなしで重要情報が見えるか

## 報告形式

以下の形式で報告してください：

### 発見した問題
1. **問題のカテゴリ**：具体的な問題の説明
2. **問題のカテゴリ**：具体的な問題の説明

### 影響度
- 致命的：使用に支障がある
- 高：ユーザー体験を大きく損なう
- 中：改善が望ましい
- 低：軽微な問題

### 推奨される対応
問題を解決するために必要な変更を簡潔に説明してください。

---

分析を開始してください。

```

### 使用例

**Before（現在）**

```markdown
添付: desktop-section-records-memo-display.png

この画像の学習記録カードで、メモが長い時にレイアウトが崩れています。
問題を修正してください。

```

**After（改善版）**

```markdown
添付: desktop-section-records-memo-display.png

[上記の汎用プロンプトを貼り付け]

```

### 期待される出力例

```markdown
### 発見した問題
1. **テキスト表示**：メモが長文の場合、カードの左半分に制限されている
2. **要素の配置**：編集・削除ボタンがメモの右側に配置され、メモが長いと下に押される
3. **視覚的な区別**：メモ部分に背景色がなく、他の情報と区別しにくい
4. **改行**：メモの改行が反映されていない可能性

### 影響度
- 致命的：メモが長いとボタンが操作しにくい（高）
- 中：メモの視認性が低い（中）

### 推奨される対応
- メモエリアをカード全幅に拡張
- ボタンを独立した行に配置
- メモに背景色を追加
- whitespace-pre-wrapで改行を維持

```

### Claude への引き継ぎテンプレート

Gemini CLIの分析完了後、私に依頼する際のテンプレート：

```markdown
## レイアウト修正依頼

### Gemini CLIの分析結果
[Gemini CLIの出力をコピペ]

### 現在のコード
```vue
[該当コードを貼り付け]
```

### ファイルパス

resources/js/views/...

上記の問題を修正してください。

```
