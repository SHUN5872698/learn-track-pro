# これまでの議論の要点まとめ（最新版：2025年11月4日更新）

## 背景・経緯

### プロジェクトの概要

- **プロジェクト名**: LearnTrack Pro - プログラミング学習管理プラットフォーム
- **技術スタック**: Laravel 12 / Vue 3 (Composition API) / Pinia / MySQL 8.0
- **構成**: モノリシックSPA
- **現在の状態**: Phase 3-4完了 → VPS本番環境構築完了、デプロイ準備完了

### 目的・目標

- 転職活動用ポートフォリオとして公開
- 実用的なDevOps経験の習得（Docker本番運用、Git戦略、自動化）
- コスト最適化を最優先

### 主要な制約

- TypeScript不使用、Bootstrap/jQuery/Options API禁止
- 個人開発のため実装工数を最小限に
- デプロイ優先、完璧主義を避ける
- **予算制約**: 月1,000-3,000円（転職活動期間3-6ヶ月）
- **Git管理対象**: `laravel-docker/src/`のみ（インフラ設定は対象外）

---

## 完了・確定事項

### プロジェクトの現在地

- ✅ Phase 0-2: アプリケーション開発完了（10/14-22）
- ✅ Phase 3-1: デプロイ先選定完了（10/23）
- ✅ Phase 3-2: VPS環境構築完了（10/25-30）
- ✅ Phase 3-3: Git戦略実装完了（10/30）
- ✅ Phase 3-4: Laravel初期化とデプロイ準備完了（11/3-4）

### 直近完了した実装（11/4）

1. ✅ Docker設定の最適化（image指定削除、dockerfile明示化）
2. ✅ BuildKitキャッシュクリーンアップ戦略の確立
3. ✅ .dockerignore作成と配置
4. ✅ Cronジョブ設定（MySQLバックアップ、キャッシュクリーンアップ）
5. ✅ VPS本番環境の最終確認

### 作成済みドキュメント

- 環境構築メモ①〜⑦（Ubuntu設定、MySQL、バックアップ、Docker、アプリ環境、Git戦略、Laravel初期化）
- 手動デプロイフロー（[manual-deploy-flow.md](http://manual-deploy-flow.md)）
- プロンプトテンプレート（手順書作成用）

---

## これまでの議論で確定した方針

### 1. Git戦略とブランチ管理

**ブランチ構成**:

- **main**: 本番環境専用
- **develop**: 開発・統合環境
- **feature**: 個別機能開発

**.gitattributes による環境別ファイル保護**:

- `config/cors.php merge=ours`: 本番設定を保護
- `.gitignore merge=ours`: .gemini/除外設定を保護

**運用ルール**:

- ローカル設定変更はdevelopでコミット
- 本番設定変更はmainで直接コミット

**判断基準**: 環境別の設定ファイルコンフリクトを防ぐ

詳細: 環境構築メモ⑥参照

### 2. Docker本番環境の構成

**リソース制限（メモリ2GB環境）**:

- PHPコンテナ: 最大1GB（limits）、最低512MB（reservations）
- MySQL（ホスト）: 512-768MB
- システム予約: 256MB

**platform設定**:

- `platform: linux/amd64` が正しい
- Xserver VPS = x86_64 = amd64

**MySQL接続**:

- `extra_hosts: host.docker.internal:host-gateway`

**判断基準**: OOM Killer発動を防ぎシステム全体の安定性維持

詳細: 環境構築メモ⑤参照

### 3. Docker Compose設定の最適化

**image指定の扱い**:

- ローカル開発環境では`image`指定を削除
- `dockerfile: Dockerfile`を明示的に指定

**判断基準**: BuildKitキャッシュの不要な蓄積を防ぎ、ビルド動作を安定化

### 4. .dockerignore設定

**配置場所**: `laravel-docker/.dockerignore`（1ファイルのみ）

**Git管理**: しない（`src/`外のため対象外）

**目的**: 不要なファイル（node_modules、storage/logs等）をビルドコンテキストから除外し、キャッシュ無効化を防ぐ

**VPSへの配置**: 手動でscpコピー（初回のみ）

**判断基準**: ビルド速度の維持とキャッシュ効率の向上

### 5. BuildKitキャッシュ管理戦略

**ローカル環境**:

- 複数プロジェクトのキャッシュ共有が必要
- ビルダー: `desktop-linux`
- クリーンアップ: 週1回、7日以上前のキャッシュ削除
- 実行方法: Makeコマンドで手動実行

**VPS環境**:

- 単一プロジェクト専用のため積極的に削除
- ビルダー: `default`
- クリーンアップ: 週1回、30日以上前のキャッシュ削除
- 実行方法: cronで自動実行

**判断基準**: 環境の特性に応じた最適なキャッシュ保持期間

### 6. Cronジョブ設定

**MySQLバックアップ**: 毎日午前4時

**Dockerキャッシュクリーンアップ**: 毎週日曜午前3時

**実行時刻の分散理由**: CPU・ディスクI/O競合を回避し、負荷を分散

**登録ユーザー**: rootユーザー（デプロイ時の初期化防止）

**判断基準**: システムリソースの効率的な利用と安定性維持

詳細: 環境構築メモ⑦参照

### 7. Docker / Docker Compose インストール

**パケットフィルター**: HTTP（80）、HTTPS（443）開放、SSH（22）閉じる

**インストール方針**:

- Docker公式リポジトリ使用
- Docker Compose v2（プラグイン形式）
- `docker compose`（ハイフンなし）使用

詳細: 環境構築メモ④参照

### 8. Makefile導入

**ローカル用**: `d-build`（ビルド+起動）、`dev-clear`（開発キャッシュクリア）、`clean-cache`（キャッシュクリーンアップ）

**VPS用**: `d-rebuild`（ビルド+起動）、`prod-cache`（本番キャッシュ）

**コマンド化しない**: マイグレーション、Composer install

**判断基準**: 安全性と柔軟性のバランス

### 9. 手動デプロイフロー

**3パターン**: コードのみ（5-10分）、依存関係（10-20分）、Dockerfile（30-60分）

### 10. 手順書作成の統一ルール

**見出し構造**:

- 見出し4（#### タイトル）は使用禁止
- 見出し3（### タイトル）まで使用
- それ以下の階層は太字（**テキスト**）で強調

**コールアウトの制約**:

- コードブロックやコマンドを入れない
- インラインコード（`APP_KEY`など）を含む簡潔な箇条書きのみ
- コマンドやURL、コードブロックが必要な説明は本文に記載

**判断基準**: 手順書の可読性と保守性の向上

### 11. MySQL自動バックアップ

**バックアップ**: cronで毎日午前4時、7日間保持

**復旧最適化**: mysqldumpに`DROP TABLE IF EXISTS`含まれる → DB初期化不要

詳細: 環境構築メモ③参照

### 12. デプロイ先選定

**結論**: Xserver VPS 2GB（月1,150円）

**AWS断念理由**: EC2+RDS月4,947円（予算の3-5倍）

**判断基準**: コスト最適化 > クラウドブランド

### 13. ハイブリッド構成

**構成**: LaravelのみDocker化、MySQLは直接インストール

**理由**:

- LaravelのみDocker化: CI/CD自動化が目的
- MySQL直接: メモリ2GBでの安定性

### 14. VPS環境構築

**SSH認証多層防御**:

- `PasswordAuthentication no` + `PermitEmptyPasswords no` + `KbdInteractiveAuthentication no`

**socket activation**: Ubuntu 24.04では無効化が正解

**fail2ban**: 動的防御でブルートフォース攻撃防止

詳細: 環境構築メモ①参照

### 15. Phase 0-2の確定方針

**Axiosインスタンス**:

- `/api/*` → `import api from '@/plugins/axios'`
- `/fortify/*`, `/sanctum/*` → `import axios from 'axios'`

**エラーハンドリング**: 再スロー必須、ストア使用直前初期化

**レスポンシブ**: `md:`のみ使用、`sm:`不使用

---

## やってはいけないこと

1. SSHパスワード認証を有効化
2. rootで直接ログイン許可
3. socket activation有効でポート変更
4. fail2banなしでSSH開放
5. MySQLのDocker化
6. バックアップ復旧時にDB削除・再作成
7. mainでローカル用設定をコミット
8. .gitattributesなしで環境別ファイル管理
9. docker-compose.ymlにimage指定とbuild指定を併記
10. BuildKitキャッシュを放置

---

## 技術選択と教訓

### 重要な技術選択の判断

1. **Xserver VPS選択**: AWS断念（コスト5倍） → 予算内実現最優先
2. **ハイブリッド構成**: Laravel Docker化 + MySQL直接 → メモリ2GB環境での安定性
3. **環境別キャッシュ戦略**: ローカルは緩め、VPSは積極的削除 → 環境特性に応じた最適化

### 重要な教訓

1. **コスト最適化最優先**: クラウドブランドより予算内実現
2. **過去経験活用**: Docker全体化複雑化経験 → ハイブリッド構成
3. **VPSでAWS機能代替可能**: cron、fail2ban等
4. **技術的背景理解重要**: BuildKitキャッシュの仕組み、ビルダーの違い、merge=ours
5. **多層防御**: 単一対策では不十分
6. **Git戦略事前設計**: 環境別ファイル問題を未然防止
7. **ドキュメント品質統一**: 見出し構造とコールアウトの制約で保守性向上
8. **環境の違いを正確に理解**: ローカルとVPSでビルダーが異なる（desktop-linux vs default）

---

## 次のアクション

### Phase 3-5: ドメイン取得とSSL証明書設定（優先度: 高）

**工数**: 1-2日

**作業内容**:

- ドメイン取得とDNS設定
- Let's Encrypt (Certbot) によるSSL証明書取得
- Apache設定変更（ServerName、ServerAlias）
- Laravel設定変更（.env、config/cors.php）
- 動作確認

### Phase 3-6: CI/CD構築（優先度: 中）

**工数**: 2-3日

**作業内容**:

- GitHub Actionsによる自動テスト
- 自動デプロイパイプライン構築
- デプロイ通知設定

---

## 特記事項

### プロジェクト健全性

- **進捗**: 優良（Phase 3-4完了、VPS本番環境構築完了）
- **技術的負債**: 最小限
- **ドキュメント**: 優良（全手順記録済み、プロンプトテンプレート完備）
- **自動化**: 良好（バックアップ、キャッシュクリーンアップ）

### デプロイまでクリティカルパス

**約1-2日**（ドメイン取得+SSL設定のみ）

---

**最終更新**: 2025年11月4日
